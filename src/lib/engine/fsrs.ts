// FSRS(Free Spaced Repetition Scheduler) 계산 — "언제 복습할지"를 담당.
// 5단계 사다리(mastery-level.ts)는 "그 순간 어떤 문항 유형을 낼지"를 담당한다.
// 두 축은 직교(orthogonal)하게 설계한다 — Stage 2 설계 문서에서 통합 규칙을 먼저 확정한 뒤 구현한다.
//
// TODO(Stage 2): stability/difficulty 갱신 공식, nextReviewAt 계산을 순수 함수로 구현하고
// vitest 단위 테스트를 먼저 작성한다(로직보다 테스트 먼저).

export {};
