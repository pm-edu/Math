// 유닛 완전학습 게이트(90%) 판정 + 교정학습(corrective loop) 큐 생성.
// 통과 조건: 유닛 단어의 90% 이상 Lv3 이상 AND 유닛 종합평가 90점 이상.
// 미통과 시 이전과 다른 문항 유형으로 교정 세션 자동 생성, 최대 3사이클 후 "집중관리 단어" 태깅.
//
// TODO(Stage 2/4): 판정 함수 + 교정 큐 선택 로직을 순수 함수로 구현.

export {};
